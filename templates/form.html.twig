{{ form_start(studentForm) }}

{% block content_body %}
    <div class="container col-md-6 ">
        <div class="card">
            <div class="card-header">Adaugare Student:</div>
            <div class="p-4">
                <div class="mb-3">
                    {{ form_row(studentForm.url, {
                        label: 'Grupa studentului: '
                    }) }}

                    <button type="submit" class="btn btn-primary">Submit</button>                </div>
            </div>
        </div>
    </div>
{% endblock %}
{{ form_end(studentForm) }}
