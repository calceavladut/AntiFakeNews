{{ form_start(articleForm) }}

{% block content_body %}
    <div class="container col-md-6 ">
        <div class="card">
            <div class="card-header">Verify:</div>
            <div class="p-4">
                <div class="mb-3">
                    {{ form_row(articleForm.url, {
                        label: 'Url: '
                    }) }}

                    <button type="submit" class="btn btn-primary">Submit</button>                </div>
            </div>
        </div>
    </div>
{% endblock %}
{{ form_end(articleForm) }}
